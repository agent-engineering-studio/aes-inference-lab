[Service]
# Su un server con piu' NIC di cui una sola cablata, il default
# "aspetta tutte le interfacce" blocca il boot per minuti.
ExecStart=
ExecStart=/usr/lib/systemd/systemd-networkd-wait-online --any --timeout=30
